#!/bin/sh
### BEGIN INIT INFO
# Provides:          leela-udp
# Required-Start:    $network $local_fs $leela-xmpp $leela-storage
# Required-Stop:
# Default-Start:     2 3 4 5
# Default-Stop:      0 1 6
# Short-Description: Distributed, real time event processor and monitoring engine
### END INIT INFO

PATH=/sbin:/usr/sbin:/bin:/usr/bin
FSROOT=${FSROOT:-/}
DAEMON=/usr/bin/twistd
SERVICE=udp
PIDFILE=/var/run/leela/udp.pid
LOGFILE=/var/log/leela/$SERVICE.log

[ -x "$DAEMON" ]                            || exit 0
[ -r /lib/lsb/init-functions ]              && . /lib/lsb/init-functions
[ -r ${FSROOT}etc/default/leela-$SERVICE ]  && . ${FSROOT}etc/default/leela-$SERVICE
[ -r ${FSROOT}usr/libexec/leela-functions ] && . ${FSROOT}usr/libexec/leela-functions

start_service () {
  log_daemon_msg "starting $DAEMON [$SERVICE]"
  start-stop-daemon -N-5 -c leela:leela -Sq -p $PIDFILE -x $DAEMON -- --logfile=$LOGFILE --pidfile=$PIDFILE leela --service=$SERVICE $DAEMON_OPTS
  log_end_msg $?
}

stop_service () {
  log_daemon_msg "stopping $DAEMON [$SERVICE]"
  start-stop-daemon -Kq -R 10 -p $PIDFILE
  log_end_msg $?
  rm -f $PIDFILE
}

restart_service () {
  stop_service
  start_service
}

case "$1" in
  start)
    start_service
    ;;

  stop)
    stop_service
    ;;

  restart)
    restart_service
    ;;

  *)
    echo "Usage: $0 {start|stop|restart}" >&2
    exit 3
    ;;
esac

exit 0
